<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body { 
      background: linear-gradient(135deg, #f5f7fa 0%, #e4e9f2 50%, #dce3f0 100%);
      font-family: 'Inter', system-ui, -apple-system, sans-serif;
      min-height: 100vh;
    }
    
    .stat-card {
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    .stat-card:hover {
      transform: translateY(-8px) scale(1.05);
      box-shadow: 0 20px 40px rgba(0,0,0,0.15);
    }
    
    html {
      scroll-behavior: smooth;
    }
  </style>
</head>
<body class="antialiased">
  <%- include('../partials/navbar') %>
  
  <div class="flex">
    <%- include('../partials/sidebar') %>
    
    <!-- Main Content with proper top spacing -->
    <main class="flex-1 ml-64 p-8 pt-32 pb-16">
      
      <!-- Welcome Header -->
      <div class="mb-10">
        <div class="bg-gradient-to-br from-purple-50 via-pink-50 to-orange-50 rounded-3xl shadow-xl p-8 border-4 border-purple-200">
          <div class="flex items-center justify-between">
            <div>
              <h1 class="text-5xl font-black bg-gradient-to-r from-purple-600 via-pink-600 to-orange-600 bg-clip-text text-transparent mb-3">
                🎯 Admin Dashboard
              </h1>
              <p class="text-gray-700 text-lg font-bold">
                Welcome back, <span class="text-purple-600"><%= user.fullName %></span>! 👋
              </p>
              <p class="text-gray-600 text-sm mt-2 font-semibold">
                📅 <%= new Date().toLocaleDateString('en-IN', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %>
              </p>
            </div>
            <div class="hidden lg:block">
              <div class="w-24 h-24 bg-gradient-to-br from-purple-500 via-pink-500 to-orange-500 rounded-2xl flex items-center justify-center text-white font-black text-4xl shadow-2xl transform hover:rotate-6 transition-transform border-4 border-white">
                <%= user.fullName.charAt(0).toUpperCase() %>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Main Stats Grid - 4 Cards (Sidebar Style) -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-10">
        
        <!-- Total Colleges Card -->
        <div class="stat-card bg-gradient-to-r from-blue-50 to-cyan-50 rounded-3xl shadow-xl border-4 border-blue-200 hover:border-blue-400 overflow-hidden">
          <div class="p-6">
            <div class="flex items-center justify-between mb-4">
              <div class="w-16 h-16 flex items-center justify-center bg-white rounded-2xl shadow-lg border-2 border-blue-300">
                <svg class="w-9 h-9 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
                </svg>
              </div>
              <div class="text-right">
                <h2 class="text-6xl font-black bg-gradient-to-br from-blue-600 to-cyan-600 bg-clip-text text-transparent">
                  <%= stats.totalColleges %>
                </h2>
              </div>
            </div>
            <p class="text-gray-700 text-sm font-bold mb-3 uppercase tracking-wider">Total Colleges</p>
            <a href="/admin/colleges" class="inline-flex items-center gap-2 bg-gradient-to-r from-blue-500 to-cyan-600 hover:from-blue-600 hover:to-cyan-700 text-white px-4 py-2.5 rounded-xl text-sm font-bold transition-all shadow-lg">
              View All 
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
            </a>
          </div>
        </div>

        <!-- Heritage Places Card -->
        <div class="stat-card bg-gradient-to-r from-green-50 to-emerald-50 rounded-3xl shadow-xl border-4 border-green-200 hover:border-green-400 overflow-hidden">
          <div class="p-6">
            <div class="flex items-center justify-between mb-4">
              <div class="w-16 h-16 flex items-center justify-center bg-white rounded-2xl shadow-lg border-2 border-green-300">
                <svg class="w-9 h-9 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                </svg>
              </div>
              <div class="text-right">
                <h2 class="text-6xl font-black bg-gradient-to-br from-green-600 to-emerald-600 bg-clip-text text-transparent">
                  <%= stats.totalPlaces %>
                </h2>
              </div>
            </div>
            <p class="text-gray-700 text-sm font-bold mb-3 uppercase tracking-wider">Heritage Places</p>
            <a href="/admin/places" class="inline-flex items-center gap-2 bg-gradient-to-r from-green-500 to-emerald-600 hover:from-green-600 hover:to-emerald-700 text-white px-4 py-2.5 rounded-xl text-sm font-bold transition-all shadow-lg">
              View All 
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
            </a>
          </div>
        </div>

        <!-- Tour Packages Card -->
        <div class="stat-card bg-gradient-to-r from-purple-50 to-pink-50 rounded-3xl shadow-xl border-4 border-purple-200 hover:border-purple-400 overflow-hidden">
          <div class="p-6">
            <div class="flex items-center justify-between mb-4">
              <div class="w-16 h-16 flex items-center justify-center bg-white rounded-2xl shadow-lg border-2 border-purple-300">
                <svg class="w-9 h-9 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"></path>
                </svg>
              </div>
              <div class="text-right">
                <h2 class="text-6xl font-black bg-gradient-to-br from-purple-600 to-pink-600 bg-clip-text text-transparent">
                  <%= stats.totalPackages %>
                </h2>
              </div>
            </div>
            <p class="text-gray-700 text-sm font-bold mb-3 uppercase tracking-wider">Tour Packages</p>
            <a href="/admin/packages" class="inline-flex items-center gap-2 bg-gradient-to-r from-purple-500 to-pink-600 hover:from-purple-600 hover:to-pink-700 text-white px-4 py-2.5 rounded-xl text-sm font-bold transition-all shadow-lg">
              View All 
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
            </a>
          </div>
        </div>

        <!-- Total Buses Card -->
        <div class="stat-card bg-gradient-to-r from-orange-50 to-red-50 rounded-3xl shadow-xl border-4 border-orange-200 hover:border-orange-400 overflow-hidden">
          <div class="p-6">
            <div class="flex items-center justify-between mb-4">
              <div class="w-16 h-16 flex items-center justify-center bg-white rounded-2xl shadow-lg border-2 border-orange-300">
                <svg class="w-9 h-9 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M8 7v8a2 2 0 002 2h6M8 7V5a2 2 0 012-2h4.586a1 1 0 01.707.293l4.414 4.414a1 1 0 01.293.707V15a2 2 0 01-2 2h-2M8 7H6a2 2 0 00-2 2v10a2 2 0 002 2h8a2 2 0 002-2v-2"></path>
                </svg>
              </div>
              <div class="text-right">
                <h2 class="text-6xl font-black bg-gradient-to-br from-orange-600 to-red-600 bg-clip-text text-transparent">
                  <%= stats.totalBuses %>
                </h2>
              </div>
            </div>
            <p class="text-gray-700 text-sm font-bold mb-3 uppercase tracking-wider">Total Buses</p>
            <a href="/admin/buses" class="inline-flex items-center gap-2 bg-gradient-to-r from-orange-500 to-red-600 hover:from-orange-600 hover:to-red-700 text-white px-4 py-2.5 rounded-xl text-sm font-bold transition-all shadow-lg">
              View All 
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
            </a>
          </div>
        </div>

      </div>

      <!-- Secondary Stats - Students -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-10">
        
        <!-- Total Students -->
        <div class="stat-card bg-white rounded-3xl shadow-xl p-6 border-4 border-indigo-200 hover:border-indigo-400 transition-all">
          <div class="flex items-center justify-between mb-3">
            <div class="w-14 h-14 flex items-center justify-center bg-gradient-to-br from-indigo-500 to-purple-600 rounded-2xl shadow-lg">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
              </svg>
            </div>
            <h3 class="text-5xl font-black bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent">
              <%= stats.totalStudents %>
            </h3>
          </div>
          <p class="text-gray-700 text-sm font-bold uppercase tracking-wider">Total Students</p>
        </div>

        <!-- Students with Packages -->
        <div class="stat-card bg-white rounded-3xl shadow-xl p-6 border-4 border-green-200 hover:border-green-400 transition-all">
          <div class="flex items-center justify-between mb-3">
            <div class="w-14 h-14 flex items-center justify-center bg-gradient-to-br from-green-500 to-emerald-600 rounded-2xl shadow-lg">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
            </div>
            <h3 class="text-5xl font-black bg-gradient-to-r from-green-600 to-emerald-600 bg-clip-text text-transparent">
              <%= stats.studentsWithPackages %>
            </h3>
          </div>
          <p class="text-gray-700 text-sm font-bold uppercase tracking-wider">With Packages</p>
        </div>

        <!-- Students with Buses -->
        <div class="stat-card bg-white rounded-3xl shadow-xl p-6 border-4 border-blue-200 hover:border-blue-400 transition-all">
          <div class="flex items-center justify-between mb-3">
            <div class="w-14 h-14 flex items-center justify-center bg-gradient-to-br from-blue-500 to-cyan-600 rounded-2xl shadow-lg">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M5 13l4 4L19 7"></path>
              </svg>
            </div>
            <h3 class="text-5xl font-black bg-gradient-to-r from-blue-600 to-cyan-600 bg-clip-text text-transparent">
              <%= stats.studentsWithBuses %>
            </h3>
          </div>
          <p class="text-gray-700 text-sm font-bold uppercase tracking-wider">With Buses</p>
        </div>

      </div>

      <!-- Recent Data Section -->
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        
        <!-- Recent Colleges -->
        <div class="bg-white rounded-3xl shadow-xl overflow-hidden border-4 border-blue-200">
          <!-- Header -->
          <div class="bg-gradient-to-r from-blue-50 to-cyan-50 p-6 border-b-4 border-blue-200">
            <div class="flex items-center justify-between">
              <div>
                <h2 class="text-2xl font-black text-gray-800 mb-1">🏫 Recent Colleges</h2>
                <p class="text-gray-600 text-sm font-semibold">Latest additions to the system</p>
              </div>
              <div class="w-12 h-12 flex items-center justify-center bg-white rounded-xl shadow-lg border-2 border-blue-300">
                <svg class="w-7 h-7 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16"></path>
                </svg>
              </div>
            </div>
          </div>
          
          <!-- Content -->
          <div class="p-6">
            <% if (recentColleges && recentColleges.length > 0) { %>
              <div class="space-y-4">
                <% recentColleges.forEach((college, index) => { %>
                  <div class="group flex items-center justify-between p-5 bg-gradient-to-r from-blue-50 to-cyan-50 rounded-2xl hover:from-blue-100 hover:to-cyan-100 transition-all shadow-md hover:shadow-xl border-2 border-blue-200 hover:border-blue-400">
                    <div class="flex items-center gap-4">
                      <div class="w-14 h-14 bg-gradient-to-br from-blue-500 to-cyan-600 rounded-xl flex items-center justify-center text-white font-black text-xl shadow-lg border-2 border-white">
                        <%= college.name.charAt(0).toUpperCase() %>
                      </div>
                      <div>
                        <p class="font-bold text-gray-800 text-lg group-hover:text-blue-600 transition-colors">
                          <%= college.name %>
                        </p>
                        <p class="text-sm text-gray-600 font-semibold mt-1">
                          📚 <%= college.departments && college.departments.length > 0 ? college.departments.length + ' departments' : 'No departments' %>
                        </p>
                      </div>
                    </div>
                    <span class="bg-gradient-to-r from-blue-500 to-cyan-600 text-white px-4 py-2 rounded-full text-sm font-bold shadow-lg">
                      🚌 <%= college.busCount || 0 %>
                    </span>
                  </div>
                <% }); %>
              </div>
            <% } else { %>
              <div class="text-center py-12">
                <div class="w-20 h-20 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-4">
                  <svg class="w-10 h-10 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16"></path>
                  </svg>
                </div>
                <p class="text-gray-500 font-semibold">No colleges found</p>
              </div>
            <% } %>
          </div>
        </div>

        <!-- Recent Packages -->
        <div class="bg-white rounded-3xl shadow-xl overflow-hidden border-4 border-purple-200">
          <!-- Header -->
          <div class="bg-gradient-to-r from-purple-50 to-pink-50 p-6 border-b-4 border-purple-200">
            <div class="flex items-center justify-between">
              <div>
                <h2 class="text-2xl font-black text-gray-800 mb-1">📦 Recent Packages</h2>
                <p class="text-gray-600 text-sm font-semibold">Latest tour packages</p>
              </div>
              <div class="w-12 h-12 flex items-center justify-center bg-white rounded-xl shadow-lg border-2 border-purple-300">
                <svg class="w-7 h-7 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M20 7l-8-4-8 4m16 0l-8 4"></path>
                </svg>
              </div>
            </div>
          </div>
          
          <!-- Content -->
          <div class="p-6">
            <% if (recentPackages && recentPackages.length > 0) { %>
              <div class="space-y-4">
                <% recentPackages.forEach((pkg, index) => { %>
                  <div class="group p-5 bg-gradient-to-r from-purple-50 to-pink-50 rounded-2xl hover:from-purple-100 hover:to-pink-100 transition-all shadow-md hover:shadow-xl border-2 border-purple-200 hover:border-purple-400">
                    <div class="flex items-start justify-between mb-3">
                      <div class="flex-1">
                        <h3 class="font-black text-gray-800 text-lg group-hover:text-purple-600 transition-colors mb-2">
                          <%= pkg.name %>
                        </h3>
                        <p class="text-sm text-gray-600 font-semibold">
                          🏫 <%= pkg.college ? pkg.college.name : 'N/A' %>
                        </p>
                      </div>
                      <span class="bg-gradient-to-r from-purple-500 to-pink-600 text-white px-4 py-2.5 rounded-full text-sm font-black shadow-lg whitespace-nowrap">
                        ₹<%= pkg.price.toLocaleString('en-IN') %>
                      </span>
                    </div>
                    <div class="flex items-center gap-4 text-sm">
                      <span class="flex items-center gap-1.5 bg-white px-3 py-2 rounded-xl font-bold text-gray-700 shadow-md border-2 border-purple-200">
                        📅 <%= pkg.duration %> days
                      </span>
                      <span class="flex items-center gap-1.5 bg-white px-3 py-2 rounded-xl font-bold text-gray-700 shadow-md border-2 border-green-200">
                        📍 <%= pkg.places ? pkg.places.length : 0 %> places
                      </span>
                    </div>
                  </div>
                <% }); %>
              </div>
            <% } else { %>
              <div class="text-center py-12">
                <div class="w-20 h-20 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-4">
                  <svg class="w-10 h-10 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4"></path>
                  </svg>
                </div>
                <p class="text-gray-500 font-semibold">No packages found</p>
              </div>
            <% } %>
          </div>
        </div>

      </div>

    </main>
  </div>
  <footer class="mt-10 border-t border-gray-200 bg-white/80 backdrop-blur">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4 flex flex-col sm:flex-row items-center justify-end gap-2 text-xs sm:text-sm text-gray-600">
    
    <p class="font-semibold text-right">
      © 2026 HTI. All rights reserved.
    </p>

    <p class="flex items-center gap-1 text-right">
      <span>Made by</span>
      <a 
        href="https://www.linkedin.com/in/ishwar-prajapati/" 
        target="_blank" 
        rel="noopener noreferrer"
        class="text-indigo-600 font-bold hover:underline hover:text-indigo-800"
      >
        Ishwar Prajapati
      </a>
    </p>
  </div>
</footer>

</body>
</html>
