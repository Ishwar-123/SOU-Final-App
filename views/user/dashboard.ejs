<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    * {
      -webkit-tap-highlight-color: transparent;
    }
    
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
      min-height: 100vh;
      font-family: 'Inter', system-ui, -apple-system, sans-serif;
      overflow-x: hidden;
    }
    
    .card-hover {
      transition: all 0.3s ease;
    }
    
    .card-hover:hover {
      transform: translateY(-5px);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
    }

    .glass-card {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.3);
    }

    .gradient-text {
      background: linear-gradient(135deg, #667eea 0%, #f093fb 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
    }

    .floating {
      animation: float 3s ease-in-out infinite;
    }
  </style>
</head>
<body>
  <%- include('../partials/navbar') %>

  <main class="max-w-7xl mx-auto px-3 sm:px-4 lg:px-8 pt-24 sm:pt-28 pb-6 sm:pb-10">
    
    <!-- Welcome Header -->
    <div class="mb-4 sm:mb-6">
      <div class="glass-card rounded-2xl sm:rounded-3xl p-4 sm:p-6 shadow-2xl">
        <div class="flex items-center gap-3 sm:gap-4">
          <div class="w-14 h-14 sm:w-20 sm:h-20 bg-gradient-to-br from-purple-600 to-pink-600 rounded-xl sm:rounded-2xl flex items-center justify-center text-white font-black text-2xl sm:text-4xl shadow-xl floating flex-shrink-0">
            <%= user.fullName.charAt(0).toUpperCase() %>
          </div>
          <div class="flex-1 min-w-0">
            <h1 class="text-xl sm:text-3xl md:text-4xl font-black gradient-text mb-0.5 sm:mb-1 truncate">
              Welcome, <%= user.fullName.split(' ')[0] %>! 👋
            </h1>
            <p class="text-xs sm:text-base text-gray-600 font-semibold truncate">
              🎓 <%= user.college ? user.college.name : 'College not assigned' %>
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- Success/Error Messages -->
    <% if (success) { %>
      <div class="mb-3 sm:mb-4 glass-card p-3 sm:p-4 rounded-xl sm:rounded-2xl shadow-lg border-l-4 border-green-500">
        <div class="flex items-center gap-2 sm:gap-3">
          <div class="w-8 h-8 sm:w-10 sm:h-10 bg-green-500 rounded-full flex items-center justify-center flex-shrink-0">
            <svg class="w-4 h-4 sm:w-6 sm:h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M5 13l4 4L19 7"></path>
            </svg>
          </div>
          <p class="font-bold text-green-700 text-xs sm:text-base"><%= success %></p>
        </div>
      </div>
    <% } %>

    <% if (error) { %>
      <div class="mb-3 sm:mb-4 glass-card p-3 sm:p-4 rounded-xl sm:rounded-2xl shadow-lg border-l-4 border-red-500">
        <div class="flex items-center gap-2 sm:gap-3">
          <div class="w-8 h-8 sm:w-10 sm:h-10 bg-red-500 rounded-full flex items-center justify-center flex-shrink-0">
            <svg class="w-4 h-4 sm:w-6 sm:h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </div>
          <p class="font-bold text-red-700 text-xs sm:text-base"><%= error %></p>
        </div>
      </div>
    <% } %>

    <!-- Stats Cards -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3 sm:gap-4 mb-4 sm:mb-6">
      
      <!-- Package Status Card -->
      <div class="glass-card rounded-2xl shadow-xl p-4 sm:p-5 card-hover border-2 border-purple-200">
        <div class="flex items-center justify-between mb-3">
          <h3 class="text-sm sm:text-base font-black text-gray-800 flex items-center gap-2">
            <span class="text-lg sm:text-xl">📦</span>
            <span>Package</span>
          </h3>
          <% if (user.selectedPackage) { %>
            <span class="bg-green-500 text-white px-2 py-1 rounded-full text-[10px] sm:text-xs font-bold shadow-md">
              ✓ DONE
            </span>
          <% } else { %>
            <span class="bg-yellow-500 text-white px-2 py-1 rounded-full text-[10px] sm:text-xs font-bold shadow-md">
              ⚠ PENDING
            </span>
          <% } %>
        </div>

        <% if (user.selectedPackage) { %>
          <div class="bg-gradient-to-r from-green-50 to-emerald-50 p-3 rounded-xl border-2 border-green-200">
            <p class="text-xs text-gray-600 mb-1">Selected Package:</p>
            <p class="text-sm sm:text-base font-bold text-green-700 truncate">
              <%= user.selectedPackage.name %>
            </p>
            <p class="text-[10px] sm:text-xs text-gray-500 mt-1">
              <%= user.selectedPackage.places.length %> places included
            </p>
            <!-- ✅ Show ONLY Start Date -->
            <% if (user.selectedPackage.startDate) { %>
              <div class="mt-2 pt-2 border-t border-green-200">
                <p class="text-[10px] text-gray-600">Tour Start Date:</p>
                <p class="text-xs font-bold text-orange-700">
                  <%= new Date(user.selectedPackage.startDate).toLocaleDateString('en-IN', { 
                    day: 'numeric', 
                    month: 'short', 
                    year: 'numeric' 
                  }) %>
                </p>
              </div>
            <% } else { %>
              <div class="mt-2 pt-2 border-t border-green-200">
                <p class="text-[10px] text-yellow-600 font-semibold">⚠️ Date not set yet</p>
              </div>
            <% } %>
          </div>
        <% } else { %>
          <div class="text-center py-2">
            <p class="text-xs sm:text-sm text-gray-600 mb-2">Not selected</p>
            <a href="/user/select-package" class="inline-block bg-gradient-to-r from-purple-500 to-pink-500 text-white font-bold py-2 px-4 rounded-xl text-xs hover:shadow-lg transition-all">
              Browse →
            </a>
          </div>
        <% } %>
      </div>

      <!-- Bus Status Card -->
      <div class="glass-card rounded-2xl shadow-xl p-4 sm:p-5 card-hover border-2 border-blue-200">
        <div class="flex items-center justify-between mb-3">
          <h3 class="text-sm sm:text-base font-black text-gray-800 flex items-center gap-2">
            <span class="text-lg sm:text-xl">🚌</span>
            <span>Bus</span>
          </h3>
          <% if (user.selectedBus) { %>
            <span class="bg-green-500 text-white px-2 py-1 rounded-full text-[10px] sm:text-xs font-bold shadow-md">
              ✓ DONE
            </span>
          <% } else { %>
            <span class="bg-yellow-500 text-white px-2 py-1 rounded-full text-[10px] sm:text-xs font-bold shadow-md">
              ⚠ PENDING
            </span>
          <% } %>
        </div>

        <% if (user.selectedBus) { %>
          <div class="bg-gradient-to-r from-blue-50 to-indigo-50 p-3 rounded-xl border-2 border-blue-200">
            <p class="text-xs text-gray-600 mb-2">Your Bus:</p>
            <p class="text-sm sm:text-base font-bold text-blue-700 truncate">
              <%= user.selectedBus.busName %>
            </p>
            <p class="text-xs text-gray-600 mt-1">
              <%= user.selectedBus.busNumber %>
            </p>
          </div>
        <% } else { %>
          <div class="text-center py-2">
            <% if (user.selectedPackage) { %>
              <p class="text-xs sm:text-sm text-gray-600 mb-2">Not selected</p>
              <a href="/user/select-bus" class="inline-block bg-gradient-to-r from-blue-500 to-cyan-500 text-white font-bold py-2 px-4 rounded-xl text-xs hover:shadow-lg transition-all">
                Select Bus →
              </a>
            <% } else { %>
              <p class="text-xs sm:text-sm text-gray-600 mb-2">Not selected</p>
              <p class="text-[10px] text-gray-500">Select package first</p>
            <% } %>
          </div>
        <% } %>
      </div>

      <!-- Total Price Card -->
      <div class="glass-card rounded-2xl shadow-xl p-4 sm:p-5 card-hover border-2 border-green-200 sm:col-span-2 lg:col-span-1">
        <div class="flex items-center justify-between mb-3">
          <h3 class="text-sm sm:text-base font-black text-gray-800 flex items-center gap-2">
            <span class="text-lg sm:text-xl">💰</span>
            <span>Total Price</span>
          </h3>
          <span class="bg-purple-500 text-white px-2 py-1 rounded-full text-[10px] sm:text-xs font-bold">
            SUMMARY
          </span>
        </div>

        <% if (user.selectedPackage) { %>
          <div class="bg-gradient-to-r from-green-50 to-emerald-50 p-3 sm:p-4 rounded-xl border-2 border-green-200">
            <p class="text-3xl sm:text-4xl font-black text-green-600 mb-2">
              ₹<%= user.selectedPackage.price.toLocaleString('en-IN') %>
            </p>
            <div class="space-y-1 text-xs sm:text-sm text-gray-600">
              <p>Package Price</p>
            </div>
          </div>
        <% } else { %>
          <div class="text-center py-4">
            <p class="text-2xl sm:text-3xl font-black text-gray-300 mb-1">₹0</p>
            <p class="text-xs sm:text-sm text-gray-500">Select package first</p>
          </div>
        <% } %>
      </div>
    </div>

    <!-- Quick Actions -->
    <div class="glass-card rounded-2xl sm:rounded-3xl p-4 sm:p-6 shadow-xl mb-4 sm:mb-6 border-2 border-indigo-200">
      <h3 class="text-lg sm:text-xl font-black text-gray-800 mb-3 sm:mb-4 flex items-center gap-2">
        <span class="text-xl sm:text-2xl">⚡</span>
        <span>Quick Actions</span>
      </h3>
      
      <div class="grid grid-cols-2 sm:grid-cols-3 gap-2 sm:gap-3">
        <% if (!user.selectedPackage) { %>
          <a href="/user/select-package" class="bg-gradient-to-r from-indigo-500 to-purple-500 text-white font-bold py-3 px-3 sm:px-4 rounded-xl hover:shadow-lg active:scale-95 transition-all text-center text-xs sm:text-sm">
            📦 Packages
          </a>
        <% } %>

        <% if (user.selectedPackage) { %>
          <a href="/user/view-receipt" target="_blank" class="bg-gradient-to-r from-green-500 to-emerald-500 text-white font-bold py-3 px-3 sm:px-4 rounded-xl hover:shadow-lg active:scale-95 transition-all text-center text-xs sm:text-sm">
            📄 Receipt
          </a>
        <% } else { %>
          <button disabled class="bg-gray-300 text-gray-500 font-bold py-3 px-3 sm:px-4 rounded-xl cursor-not-allowed text-center text-xs sm:text-sm">
            📄 Receipt
          </button>
        <% } %>

        <a href="/auth/logout" class="bg-gradient-to-r from-red-500 to-pink-500 text-white font-bold py-3 px-3 sm:px-4 rounded-xl hover:shadow-lg active:scale-95 transition-all text-center text-xs sm:text-sm">
          🚪 Logout
        </a>
      </div>
    </div>

    <!-- Package Details (If Selected) -->
    <% if (user.selectedPackage) { %>
      
      <div class="glass-card rounded-2xl sm:rounded-3xl shadow-2xl overflow-hidden mb-4 sm:mb-6 border-2 border-purple-200">
        <div class="bg-gradient-to-r from-indigo-600 via-purple-600 to-pink-600 p-4 sm:p-6 text-white">
          <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-3">
            <div>
              <h2 class="text-xl sm:text-2xl md:text-3xl font-black mb-1">🏛️ Your Package</h2>
              <p class="text-xs sm:text-sm text-indigo-100">Heritage tour selection</p>
            </div>
            <a href="/user/view-receipt" target="_blank" class="w-full sm:w-auto bg-white text-purple-600 font-bold py-2 sm:py-3 px-4 sm:px-6 rounded-xl hover:shadow-xl active:scale-95 transition-all flex items-center justify-center gap-2 text-xs sm:text-sm">
              <span class="text-base sm:text-lg">📄</span>
              <span>View Receipt</span>
            </a>
          </div>
        </div>

        <div class="p-4 sm:p-6">
          <div class="mb-4">
            <h3 class="text-xl sm:text-2xl font-black text-gray-800 mb-1">
              <%= user.selectedPackage.name %>
            </h3>
            <p class="text-xs sm:text-sm text-gray-600"><%= user.selectedPackage.description %></p>
          </div>

          <div class="grid grid-cols-2 gap-2 sm:gap-4 mb-4 sm:mb-6">
            <div class="bg-gradient-to-br from-blue-50 to-cyan-50 p-3 sm:p-4 rounded-xl border-2 border-blue-200 shadow-sm text-center">
              <p class="text-[10px] sm:text-xs text-blue-600 font-bold mb-1">⏱️ Duration</p>
              <p class="text-lg sm:text-2xl font-black text-blue-800"><%= user.selectedPackage.duration %></p>
              <p class="text-[10px] sm:text-xs text-blue-600">Days</p>
            </div>

            <div class="bg-gradient-to-br from-purple-50 to-purple-100 p-3 sm:p-4 rounded-xl border-2 border-purple-200 shadow-sm text-center">
              <p class="text-[10px] sm:text-xs text-purple-600 font-bold mb-1">📍 Places</p>
              <p class="text-lg sm:text-2xl font-black text-purple-800"><%= user.selectedPackage.places.length %></p>
              <p class="text-[10px] sm:text-xs text-purple-600">Sites</p>
            </div>
            
            <!-- ✅ Show ONLY Start Date -->
            <% if (user.selectedPackage.startDate) { %>
              <div class="bg-gradient-to-br from-orange-50 to-red-50 p-3 sm:p-4 rounded-xl border-2 border-orange-200 shadow-sm col-span-2">
                <p class="text-[10px] sm:text-xs text-orange-600 font-bold mb-1 text-center">📅 Tour Start Date</p>
                <p class="text-base sm:text-xl font-black text-orange-700 text-center">
                  <%= new Date(user.selectedPackage.startDate).toLocaleDateString('en-IN', { 
                    weekday: 'long',
                    day: 'numeric', 
                    month: 'long', 
                    year: 'numeric' 
                  }) %>
                </p>
                <p class="text-[10px] sm:text-xs text-gray-600 text-center mt-1">
                  Duration: <%= user.selectedPackage.duration %> days tour
                </p>
              </div>
            <% } %>

            <div class="bg-gradient-to-br from-green-50 to-emerald-50 p-3 sm:p-4 rounded-xl border-2 border-green-200 shadow-sm col-span-2 text-center">
              <p class="text-[10px] sm:text-xs text-green-600 font-bold mb-1">💰 Package Price</p>
              <p class="text-xl sm:text-3xl font-black text-green-800">₹<%= user.selectedPackage.price.toLocaleString('en-IN') %></p>
            </div>
          </div>

          <div class="mb-4">
            <h3 class="text-base sm:text-lg font-black text-gray-800 mb-3 flex items-center gap-2">
              <span>🏛️</span>
              <span>Heritage Sites (<%= user.selectedPackage.places.length %>)</span>
            </h3>
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-2 sm:gap-3">
              <% user.selectedPackage.places.forEach((place, index) => { %>
                <div class="bg-gradient-to-br from-purple-50 to-pink-50 p-3 sm:p-4 rounded-xl border-2 border-purple-200">
                  <div class="flex items-start gap-2 mb-2">
                    <span class="bg-purple-600 text-white w-6 h-6 sm:w-7 sm:h-7 rounded-full flex items-center justify-center font-bold text-xs flex-shrink-0">
                      <%= index + 1 %>
                    </span>
                    <div class="flex-1 min-w-0">
                      <p class="font-bold text-purple-800 text-sm sm:text-base truncate"><%= place.name %></p>
                      <p class="text-[10px] sm:text-xs text-gray-600 truncate">📍 <%= place.location %></p>
                    </div>
                  </div>
                  <div class="text-right">
                    <span class="bg-green-500 text-white px-2 py-1 rounded-full text-[10px] sm:text-xs font-bold">
                      ₹<%= place.price.toLocaleString('en-IN') %>
                    </span>
                  </div>
                </div>
              <% }); %>
            </div>
          </div>

          <div class="bg-gradient-to-r from-indigo-50 to-purple-50 p-3 sm:p-4 rounded-xl border-2 border-indigo-200">
            <h4 class="font-bold text-indigo-800 mb-2 text-xs sm:text-sm">📋 Booking Info</h4>
            <div class="grid grid-cols-2 gap-2 text-[10px] sm:text-xs">
              <p class="text-gray-700"><span class="font-bold">ID:</span> PKG-<%= user.selectedPackage._id.toString().slice(-6).toUpperCase() %></p>
              <p class="text-gray-700"><span class="font-bold">Date:</span> <%= new Date(user.packageSelectedAt).toLocaleDateString('en-IN') %></p>
              <p class="text-gray-700 col-span-2"><span class="font-bold">Status:</span> <span class="text-green-600 font-bold">✓ Confirmed</span></p>
            </div>
          </div>
        </div>
      </div>

    <% } else { %>
      
      <!-- No Package Selected State -->
      <div class="glass-card rounded-2xl sm:rounded-3xl shadow-2xl p-8 sm:p-12 text-center border-2 border-gray-200">
        <div class="text-6xl sm:text-8xl mb-4">📦</div>
        <h2 class="text-2xl sm:text-3xl md:text-4xl font-black text-gray-800 mb-2 sm:mb-3">
          No Package Selected
        </h2>
        <p class="text-sm sm:text-base text-gray-600 mb-4 sm:mb-6 max-w-xl mx-auto">
          Explore our curated heritage tourism packages and embark on a journey through India's rich cultural heritage.
        </p>
        <a href="/user/select-package" class="inline-block bg-gradient-to-r from-purple-600 via-pink-600 to-red-600 text-white font-bold py-3 sm:py-4 px-6 sm:px-10 rounded-xl sm:rounded-2xl hover:shadow-2xl active:scale-95 transition-all text-sm sm:text-base">
          🎯 Browse All Packages
        </a>
      </div>
      
    <% } %>

    <!-- Info Footer -->
    <!-- <div class="glass-card rounded-2xl p-4 sm:p-5 text-center border-2 border-white/30">
      <p class="text-xs sm:text-sm text-gray-700">
        💡 <strong>Need Help?</strong> Contact us at 
        <a href="mailto:support@missionunity.com" class="underline font-bold text-purple-600">support@missionunity.com</a>
      </p>
    </div> -->

  </main>
  <%- include('../partials/footer') %>
</body>
</html>
