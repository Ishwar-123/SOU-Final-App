<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    * {
      -webkit-tap-highlight-color: transparent;
    }
    
    body { 
      background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
      min-height: 100vh;
      font-family: 'Inter', system-ui, -apple-system, sans-serif;
      overflow-x: hidden;
    }
    
    .glass-card {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.3);
    }
    
    .floating {
      animation: float 6s ease-in-out infinite;
    }
    
    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-15px); }
    }
    
    .gradient-text {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .bg-circle {
      position: absolute;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.1);
      animation: pulse 4s ease-in-out infinite;
      pointer-events: none;
    }
    
    @keyframes pulse {
      0%, 100% { transform: scale(1); opacity: 0.5; }
      50% { transform: scale(1.1); opacity: 0.3; }
    }

    html {
      scroll-behavior: smooth;
    }

    input:focus {
      transform: scale(1.02);
      transition: all 0.2s ease;
    }
  </style>
</head>
<body class="min-h-screen flex flex-col justify-between p-3 sm:p-4">
  
  <!-- Animated Background Circles -->
  <div class="bg-circle w-40 h-40 sm:w-64 sm:h-64 top-10 left-5 sm:left-10" style="animation-delay: 0s;"></div>
  <div class="bg-circle w-60 h-60 sm:w-96 sm:h-96 bottom-10 right-5 sm:right-10" style="animation-delay: 2s;"></div>
  <div class="bg-circle w-32 h-32 sm:w-48 sm:h-48 top-1/2 right-1/4" style="animation-delay: 4s;"></div>

  <!-- MAIN CONTENT (centered block) -->
  <div class="w-full max-w-md mx-auto relative z-10 flex-1 flex items-center">
    <div class="w-full">
      <!-- Header -->
      <div class="text-center mb-6 sm:mb-8">
        <div class="w-16 h-16 sm:w-20 sm:h-20 bg-white/20 backdrop-blur-md rounded-2xl flex items-center justify-center mx-auto mb-3 sm:mb-4 shadow-2xl floating">
          <img src="/images/logo.png" alt="Logo" class="w-12 h-12 sm:w-14 sm:h-14 object-contain">
        </div>
        <h1 class="text-3xl sm:text-4xl font-black text-white mb-1 sm:mb-2">Mission Unity</h1>
        <p class="text-sm sm:text-base text-white/90 font-semibold">🎓 Lessons in Leadership</p>
      </div>

      <div class="glass-card rounded-3xl p-6 sm:p-8 md:p-10 shadow-2xl">
        
        <!-- Title -->
        <div class="text-center mb-6 sm:mb-8">
          <h2 class="text-3xl sm:text-4xl font-black gradient-text mb-1 sm:mb-2">Welcome Back!</h2>
          <p class="text-sm sm:text-base text-gray-600 font-semibold">Login to continue your journey</p>
        </div>

        <!-- Error Message -->
        <% if (error) { %>
          <div class="mb-4 sm:mb-6 p-3 sm:p-4 bg-gradient-to-r from-red-50 to-pink-50 border-l-4 border-red-500 rounded-xl shadow-lg">
            <div class="flex items-center gap-2 sm:gap-3">
              <div class="w-7 h-7 sm:w-8 sm:h-8 bg-red-500 rounded-full flex items-center justify-center flex-shrink-0">
                <svg class="w-4 h-4 sm:w-5 sm:h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
              </div>
              <p class="text-red-700 font-bold text-sm sm:text-base"><%= error %></p>
            </div>
          </div>
        <% } %>

        <!-- Login Form -->
        <form action="/auth/login" method="POST" class="space-y-4 sm:space-y-5">
          
          <!-- Email -->
          <div>
            <label for="email" class="block text-gray-800 font-black mb-1.5 sm:mb-2 text-xs sm:text-sm uppercase tracking-wider">
              📧 Email Address
            </label>
            <div class="relative">
              <div class="absolute inset-y-0 left-0 pl-3 sm:pl-4 flex items-center pointer-events-none">
                <svg class="w-4 h-4 sm:w-5 sm:h-5 text-purple-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.207"></path>
                </svg>
              </div>
              <input 
                type="email" 
                id="email" 
                name="email" 
                required
                class="w-full pl-10 sm:pl-12 pr-3 sm:pr-4 py-2.5 sm:py-3.5 text-sm sm:text-base border-2 sm:border-3 border-purple-200 rounded-xl sm:rounded-2xl focus:border-purple-500 focus:ring-2 sm:focus:ring-4 focus:ring-purple-100 focus:outline-none font-semibold text-gray-800 shadow-sm transition-all duration-300"
                placeholder="your.email@example.com"
              >
            </div>
          </div>

          <!-- Password -->
          <div>
            <label for="password" class="block text-gray-800 font-black mb-1.5 sm:mb-2 text-xs sm:text-sm uppercase tracking-wider">
              🔒 Password
            </label>
            <div class="relative">
              <div class="absolute inset-y-0 left-0 pl-3 sm:pl-4 flex items-center pointer-events-none">
                <svg class="w-4 h-4 sm:w-5 sm:h-5 text-purple-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
                </svg>
              </div>
              <input 
                type="password" 
                id="password" 
                name="password" 
                required
                minlength="6"
                class="w-full pl-10 sm:pl-12 pr-3 sm:pr-4 py-2.5 sm:py-3.5 text-sm sm:text-base border-2 sm:border-3 border-purple-200 rounded-xl sm:rounded-2xl focus:border-purple-500 focus:ring-2 sm:focus:ring-4 focus:ring-purple-100 focus:outline-none font-semibold text-gray-800 shadow-sm transition-all duration-300"
                placeholder="••••••••"
              >
            </div>
          </div>

          <!-- Submit Button -->
          <button 
            type="submit" 
            class="w-full bg-gradient-to-r from-purple-600 via-indigo-600 to-purple-600 hover:from-purple-700 hover:via-indigo-700 hover:to-purple-700 text-white font-black py-3 sm:py-4 rounded-2xl shadow-xl hover:shadow-2xl active:scale-95 transition-all duration-300 text-base sm:text-lg"
          >
            <span class="flex items-center justify-center gap-2">
              <svg class="w-4 h-4 sm:w-5 sm:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"></path>
              </svg>
              Sign In
            </span>
          </button>
        </form>

        <!-- Divider -->
        <div class="relative my-5 sm:my-6">
          <div class="absolute inset-0 flex items-center">
            <div class="w-full border-t-2 border-gray-200"></div>
          </div>
          <div class="relative flex justify-center text-xs sm:text-sm">
            <span class="px-3 sm:px-4 bg-white text-gray-500 font-bold">Don't have an account?</span>
          </div>
        </div>

        <!-- Register Link -->
        <a 
          href="/auth/register" 
          class="block w-full text-center bg-gradient-to-r from-gray-100 to-gray-200 hover:from-gray-200 hover:to-gray-300 active:scale-95 text-gray-800 font-black py-3 sm:py-4 rounded-2xl shadow-lg hover:shadow-xl transition-all duration-300 text-sm sm:text-base"
        >
          <span class="flex items-center justify-center gap-2">
            <svg class="w-4 h-4 sm:w-5 sm:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"></path>
            </svg>
            Create New Account
          </span>
        </a>
      </div>

      <!-- Footer Note -->
      <p class="text-center text-white/80 mt-4 sm:mt-6 text-xs sm:text-sm font-semibold">
        🔐 Your data is secure and encrypted
      </p>
    </div>
  </div>

  <!-- GLOBAL FOOTER (BOTTOM) -->
  <footer class="mt-6 border-t border-gray-200 bg-white/80 backdrop-blur relative z-10">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-3 flex flex-col sm:flex-row items-center justify-center gap-2 text-xs sm:text-sm text-gray-600">
      <p class="font-semibold text-right">
        © 2026 HTI. All rights reserved.
      </p>
      <p class="flex items-center gap-1 text-right">
        <span>Made by</span>
        <a 
          href="https://www.linkedin.com/in/ishwar-prajapati/" 
          target="_blank" 
          rel="noopener noreferrer"
          class="text-indigo-600 font-bold hover:underline hover:text-indigo-800"
        >
          Ishwar Prajapati
        </a>
      </p>
    </div>
  </footer>

  <script>
    // Prevent zoom on input focus (iOS)
    document.querySelectorAll('input').forEach(element => {
      element.addEventListener('focus', function() {
        this.style.fontSize = window.innerWidth < 640 ? '16px' : '';
      });
    });
  </script>
</body>
</html>
